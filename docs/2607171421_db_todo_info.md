# Нотатки щодо проектування структури БД

Ось придумав таку систему доступів та привілеїв.
Дай оцінку, зауваження та рекомендації щодо реалізації.

## що зараз маємо
```text
users 
	id 
	username 
	"password" 
	role_id
roles 
	id
	"name"
privileges
	id 
	"name"
role_privileges 
	role_id 
	privilege_id
```
## що додаємо
```text
Таблиці
groups
	id
    "name"
    descr
    is_active
    date/user
user_groups
    id
    user_id
    group_id
    descr
    date/user

Ролі можуть будуть наступні :
    - admin - повний доступ
    - editor - доступ на редагування іконок, шаблонів, розділів
    - user - перегляд іконок та шаблонів (можливо інших налаштувань), та робота з Розділами і їх змістом
    - viewer - тільки перегляд розділів

Привілеї для ролей будуть наступні :
    - admin - редачування користувачів, ролей, привілеїв та глобальних налаштувань якихось в майбутньому.
    - admin-view - перегляд користувачів, ролей, привілеїв та глобальних налаштувань якихось в майбутньому.
    - icons-edit - редагування іконок
    - icons-view - перегляд іконок
    - logs-view
    - sections-full - повний доступ до всіх розділів
    - sections-edit
    - sections-view
    - section_categories-edit
    - section_categories-view
    - section_document_info_block_styles-edit
    - section_document_info_block_styles-view
    - templates-edit
    - templates-view

Групи для початку можуть бути такі :
    - Розробники
    - Тестери
    - Супровід
    - Менеджери
    - Бухгалтери

Ролі вказують куди і який буде доступ. Наприклад доступ до іконок тільки на перегляд.
Групи вказують саме до яких елентів є доступ. Наприклад у конкретної групи є доступ до розділу "Документація для розробників" на читання, 
а другої групи на цей розділ є права на редагування.

Коли юзер створює Розділ другого рівня (від загального кореня) він вказує які Групи до яких він входить мають сюди дуступ і який.
При створені підрозділів цього розділу вони успадковують налаштування доступу свого предка і можуть змінюватись тільки в сторону зменшення доступу.




```


## Усіляке
=============== в КБейсі в чаті

### 7. `show_level` — вузьке рішення для прав доступу

Поле `show_level int` (0=приватний, 1=зареєстровані, 2=публічний) є простим, але не масштабованим. У проекті вже є таблиці `roles` і `privileges`.

**Рекомендація:** Розглянути зв'язок між `sections` та `roles`:
```sql
kbase.section_access  -- таблиця прав доступу до розділів
    section_id bigint,
    role_id bigint,
    access_level smallint  -- 1=read, 2=write, 3=delete
```
Або залишити `show_level` як "базовий рівень" і додати більш гранульовані права пізніше — це також прийнятне рішення для MVP.

### Немотрон
#### 3. **`show_level` в `sections` — обмежений контроль доступу** (згідно ваших нотаток)
Поточне: `int DEFAULT 0` (0=private, 1=registered, 2=public).
**Рекомендація:** Залишити як "базовий рівень" для MVP, але підготувати грунт для `section_access`:
```sql
-- Майбутня таблиця (поки не створювати, але мати в плані)
CREATE TABLE kbase.section_access (
    section_id BIGINT REFERENCES kbase.sections(id) ON DELETE CASCADE,
    role_id    BIGINT REFERENCES kbase.roles(id) ON DELETE CASCADE,
    access_level SMALLINT NOT NULL CHECK (access_level IN (1,2,3)), -- 1=read, 2=write, 3=admin
    PRIMARY KEY (section_id, role_id)
);
```
Логіка: `show_level` = fallback, `section_access` = granular override.


--<< end

