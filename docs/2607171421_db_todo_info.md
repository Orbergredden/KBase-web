# Нотатки щодо проектування структури БД
## поточне
```text




- зробити типу info_block_styles : налаштовувати шаблон та можливо іконку



dict
documents
```

## Категорії
```text
- розробити структуру Категорій
- sections.template_main - переносимо в Категорії Розділів
```

## Усіляке
```text
/*
-? можливо всі компоненти зберігати не в трьох таблиція а в одній. Наскільки це затратно по розмірам ?

Missing Tables (referenced but not defined)
Table	Referenced by
info_block_style	info_block_headers.style_id
info_block_component_type	info_block_type_components.info_block_component_type_id

Current Issues Visualized
│  ❌ No UNIQUE(position) on info_block_headers 
*/
```
**Спільний `seq_info`** для 4 різних таблиць | Окремі sequences або `GENERATED ALWAYS AS IDENTITY` |  
**`bytea` в БД** для картинок та файлів | Гібрид: `storage_type` + `file_path` для зовнішнього сховища |  
**`bytea` в БД** для картинок та файлів | Гібрид: `storage_type` + `file_path` для зовнішнього сховища |
| 6 | **`info_block_type`** замість **`info_block_type_id`** | Неконсистентне іменування FK |
| 7 | **`show_level`** — обмежений контроль доступу | Розглянути таблицю `section_access` + зв'язок з `roles` |

-------------------------------
# 🔑 Найважливіша рекомендація — зберігання файлів

Поточний `bytea` у БД спричинить **значне зростання розміру БД та зниження продуктивності**. Рекомендую гібридний підхід:

```sql
storage_type smallint DEFAULT 1,  -- 1=DB, 2=FileSystem, 3=S3
value bytea,                       -- для малих / inline
file_path varchar(500)             -- для зовнішнього сховища
```

Це дозволить спочатку зберігати в БД, а потім мігрувати без зміни схеми.
---------------------------------
-----------------------------------------
# Sonnet - Аналіз схеми БД: sections_tables_schema.md






# В майбутньому робимо
- перемикати Теми мабудь окремо від тем сайту десь в налащтуваннях, а разом із сайтом тульки синхронізувати світлий та темний вид теми